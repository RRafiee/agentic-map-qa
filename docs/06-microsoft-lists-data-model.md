# Microsoft Lists Data Model

**Project:** Agentic MAP-QA  
**Document version:** 0.1  
**Status:** Initial design  

## 1. Purpose

This document defines the Microsoft Lists data structure for collecting, validating and managing module assessment plans. The design replaces the previous fixed Microsoft Forms structure with a relational model that supports a variable number of assessment components and produces assessment-level records compatible with the target MAP output.